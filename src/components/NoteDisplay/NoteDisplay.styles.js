import { StyleSheet } from "react-native";
import { colors } from "../../utils/colors";

export const NoteDisplayStyles = StyleSheet.create({
  mainContainer: {
    height: 250,
    width: "100%",
    backgroundColor: "white",
    paddingHorizontal: 20,
    borderBottomColor: "#999",
    borderBottomWidth: 7,
  },
});
